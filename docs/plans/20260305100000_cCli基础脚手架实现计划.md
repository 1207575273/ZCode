# cCli 基础脚手架实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 从零搭建 cCli 项目骨架，使 `pnpm dev` 可以启动一个最小化的 CLI 界面，并且测试基础设施就绪。

**Architecture:** Node.js 20 + TypeScript strict 模式，Ink 5 做终端 UI，LangChain 做 LLM 调用层（不用 LangGraph），Agent Loop 完全自研。项目结构按功能分层：core / providers / tools / ui / platform / observability / persistence / commands / mcp / skills / dashboard。

**Tech Stack:** Node.js 20, TypeScript 5, pnpm, Ink 5, React 18, @langchain/openai, @langchain/anthropic, execa, fast-glob, better-sqlite3, Vitest, tsup

---

## Task 1: 项目基础文件

**Files:**
- Create: `package.json`
- Create: `.nvmrc`
- Create: `tsconfig.json`
- Create: `tsconfig.build.json`

**Step 1: 创建 .nvmrc**

```bash
echo "20" > .nvmrc
```

**Step 2: 创建 package.json**

```json
{
  "name": "ccode",
  "version": "0.1.0",
  "description": "多模型 AI 编程 CLI 助手",
  "type": "module",
  "bin": {
    "ccode": "./dist/bin/ccli.js"
  },
  "scripts": {
    "dev": "tsx bin/ccli.ts",
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": ["ai", "cli", "llm", "claude", "openai"],
  "license": "MIT"
}
```

**Step 3: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "verbatimModuleSyntax": true,
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".",
    "paths": {
      "@core/*": ["src/core/*"],
      "@providers/*": ["src/providers/*"],
      "@tools/*": ["src/tools/*"],
      "@ui/*": ["src/ui/*"],
      "@platform/*": ["src/platform/*"],
      "@commands/*": ["src/commands/*"],
      "@persistence/*": ["src/persistence/*"],
      "@observability/*": ["src/observability/*"]
    },
    "outDir": "dist",
    "rootDir": ".",
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*", "bin/**/*", "tests/**/*"],
  "exclude": ["node_modules", "dist", "a_res"]
}
```

**Step 4: 创建 tsconfig.build.json** (构建时排除测试)

```json
{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "dist", "tests", "a_res"]
}
```

**Step 5: Commit**

```bash
git add .nvmrc package.json tsconfig.json tsconfig.build.json
git commit -m "chore: 项目基础配置文件 (package.json / tsconfig / .nvmrc)"
```

---

## Task 2: 目录骨架

**Files:**
- Create: `bin/ccli.ts` (占位)
- Create: `src/core/.gitkeep`
- Create: `src/providers/.gitkeep`
- Create: `src/tools/.gitkeep`
- Create: `src/ui/.gitkeep`
- Create: `src/platform/.gitkeep`
- Create: `src/commands/.gitkeep`
- Create: `src/persistence/.gitkeep`
- Create: `src/observability/.gitkeep`
- Create: `src/mcp/.gitkeep`
- Create: `src/skills/.gitkeep`
- Create: `src/dashboard/.gitkeep`
- Create: `tests/unit/.gitkeep`
- Create: `tests/integration/.gitkeep`

**Step 1: 批量创建目录**

```bash
mkdir -p bin
mkdir -p src/{core,providers,tools,ui,platform,commands,persistence,observability,mcp,skills,dashboard}
mkdir -p tests/{unit,integration}
mkdir -p dashboard-ui/src
```

**Step 2: 创建占位入口 bin/ccli.ts**

```typescript
// bin/ccli.ts — CLI 入口 (后续 Task 7 完整实现)
console.log('cCli starting...')
```

**Step 3: 为空目录添加 .gitkeep**

```bash
find src -type d -empty -exec touch {}/.gitkeep \;
find tests -type d -empty -exec touch {}/.gitkeep \;
touch dashboard-ui/src/.gitkeep
```

**Step 4: Commit**

```bash
git add bin/ src/ tests/ dashboard-ui/
git commit -m "chore: 创建项目目录骨架"
```

---

## Task 3: 安装核心依赖

**Step 1: 安装运行时依赖**

```bash
pnpm add \
  ink@^5 \
  react@^18 \
  @langchain/openai \
  @langchain/anthropic \
  @langchain/google-genai \
  @langchain/core \
  @modelcontextprotocol/sdk \
  execa \
  fast-glob \
  better-sqlite3 \
  chalk \
  marked \
  hono \
  zod
```

**Step 2: 安装开发依赖**

```bash
pnpm add -D \
  typescript \
  tsx \
  tsup \
  vitest \
  @vitest/coverage-v8 \
  @types/node \
  @types/react \
  @types/better-sqlite3 \
  @types/marked
```

**Step 3: 验证安装**

```bash
pnpm list --depth 0
```

预期: 能看到 ink、react、@langchain/* 等包列出

**Step 4: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: 安装核心依赖"
```

---

## Task 4: 构建与测试基础配置

**Files:**
- Create: `tsup.config.ts`
- Create: `vitest.config.ts`

**Step 1: 创建 tsup.config.ts**

```typescript
import { defineConfig } from 'tsup'

export default defineConfig({
  entry: ['bin/ccli.ts'],
  outDir: 'dist',
  format: ['esm'],
  target: 'node20',
  splitting: false,
  sourcemap: true,
  clean: true,
  banner: {
    js: '#!/usr/bin/env node',
  },
  tsconfig: 'tsconfig.build.json',
})
```

**Step 2: 创建 vitest.config.ts**

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.d.ts'],
    },
  },
  resolve: {
    alias: {
      '@core': new URL('./src/core', import.meta.url).pathname,
      '@providers': new URL('./src/providers', import.meta.url).pathname,
      '@tools': new URL('./src/tools', import.meta.url).pathname,
      '@ui': new URL('./src/ui', import.meta.url).pathname,
      '@platform': new URL('./src/platform', import.meta.url).pathname,
      '@commands': new URL('./src/commands', import.meta.url).pathname,
      '@persistence': new URL('./src/persistence', import.meta.url).pathname,
      '@observability': new URL('./src/observability', import.meta.url).pathname,
    },
  },
})
```

**Step 3: 写第一个占位测试（验证测试基础设施可用）**

创建 `tests/unit/sanity.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'

describe('sanity', () => {
  it('测试基础设施正常工作', () => {
    expect(1 + 1).toBe(2)
  })
})
```

**Step 4: 运行测试**

```bash
pnpm test
```

预期: `1 passed`

**Step 5: Commit**

```bash
git add tsup.config.ts vitest.config.ts tests/unit/sanity.test.ts
git commit -m "chore: 构建与测试基础配置 (tsup / vitest)"
```

---

## Task 5: 核心类型定义

**Files:**
- Create: `src/core/types.ts` — Message、Role、ToolCall 等基础类型
- Create: `src/providers/provider.ts` — LLMProvider 接口定义
- Create: `src/tools/types.ts` — Tool 接口定义
- Test: `tests/unit/core/types.test.ts`

**Step 1: 创建 src/core/types.ts**

```typescript
// src/core/types.ts

export type Role = 'user' | 'assistant' | 'system' | 'tool'

export interface TextContent {
  type: 'text'
  text: string
}

export interface ToolCallContent {
  type: 'tool_call'
  toolCallId: string
  toolName: string
  args: Record<string, unknown>
}

export interface ToolResultContent {
  type: 'tool_result'
  toolCallId: string
  result: unknown
  isError?: boolean
}

export type MessageContent = TextContent | ToolCallContent | ToolResultContent

export interface Message {
  role: Role
  content: MessageContent | MessageContent[] | string
  id?: string
  createdAt?: Date
}

export interface TokenUsage {
  inputTokens: number
  outputTokens: number
  cacheReadTokens: number
  cacheWriteTokens: number
}

export interface StreamChunk {
  type: 'text' | 'tool_call' | 'usage' | 'done' | 'error'
  text?: string
  toolCall?: ToolCallContent
  usage?: TokenUsage
  error?: string
}
```

**Step 2: 创建 src/providers/provider.ts**

```typescript
// src/providers/provider.ts

import type { Message, StreamChunk, TokenUsage } from '@core/types.js'

export interface ChatRequest {
  model: string
  messages: Message[]
  tools?: ToolDefinition[]
  maxTokens?: number
  temperature?: number
  systemPrompt?: string
  signal?: AbortSignal
}

export interface ToolDefinition {
  name: string
  description: string
  parameters: Record<string, unknown>  // JSON Schema
}

export type ProviderProtocol = 'openai-compat' | 'native-anthropic' | 'native-google'

export interface ProviderConfig {
  name: string
  protocol: ProviderProtocol
  baseURL?: string
  apiKey: string
  models: string[]
}

export interface LLMProvider {
  readonly name: string
  readonly protocol: ProviderProtocol

  chat(request: ChatRequest): AsyncIterable<StreamChunk>
  countTokens(messages: Message[]): Promise<number>
  isModelSupported(model: string): boolean
}
```

**Step 3: 创建 src/tools/types.ts**

```typescript
// src/tools/types.ts

export interface ToolContext {
  cwd: string
  signal?: AbortSignal
}

export interface ToolResult {
  success: boolean
  output: string
  error?: string
}

export interface Tool {
  readonly name: string
  readonly description: string
  readonly parameters: Record<string, unknown>  // JSON Schema

  execute(args: Record<string, unknown>, ctx: ToolContext): Promise<ToolResult>
}
```

**Step 4: 写类型测试**

创建 `tests/unit/core/types.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import type { Message, TokenUsage, StreamChunk } from '@core/types.js'

describe('core types', () => {
  it('Message 类型结构正确', () => {
    const msg: Message = {
      role: 'user',
      content: 'hello',
    }
    expect(msg.role).toBe('user')
  })

  it('TokenUsage 四维字段齐全', () => {
    const usage: TokenUsage = {
      inputTokens: 100,
      outputTokens: 50,
      cacheReadTokens: 20,
      cacheWriteTokens: 5,
    }
    expect(Object.keys(usage)).toHaveLength(4)
  })

  it('StreamChunk type 枚举完整', () => {
    const types: StreamChunk['type'][] = ['text', 'tool_call', 'usage', 'done', 'error']
    expect(types).toHaveLength(5)
  })
})
```

**Step 5: 运行测试**

```bash
pnpm test
```

预期: `4 passed`

**Step 6: Commit**

```bash
git add src/core/types.ts src/providers/provider.ts src/tools/types.ts tests/unit/core/
git commit -m "feat: 核心类型定义 (Message / TokenUsage / LLMProvider / Tool 接口)"
```

---

## Task 6: Platform Detector (Shell 自适应)

**Files:**
- Create: `src/platform/detector.ts` — OS 检测
- Create: `src/platform/shell-resolver.ts` — Shell 选择 + Git Bash 探测
- Test: `tests/unit/platform/detector.test.ts`
- Test: `tests/unit/platform/shell-resolver.test.ts`

**Step 1: 创建 src/platform/detector.ts**

```typescript
// src/platform/detector.ts
import os from 'node:os'

export type Platform = 'win32' | 'linux' | 'darwin'

export interface PlatformInfo {
  platform: Platform
  isWindows: boolean
  isLinux: boolean
  isMac: boolean
  arch: string
  homeDir: string
  ccodeDir: string
}

export function detectPlatform(): PlatformInfo {
  const platform = os.platform() as Platform
  return {
    platform,
    isWindows: platform === 'win32',
    isLinux: platform === 'linux',
    isMac: platform === 'darwin',
    arch: os.arch(),
    homeDir: os.homedir(),
    ccodeDir: `${os.homedir()}/.ccode`,
  }
}
```

**Step 2: 创建 src/platform/shell-resolver.ts**

```typescript
// src/platform/shell-resolver.ts
import { existsSync } from 'node:fs'
import { detectPlatform } from './detector.js'

export type ShellType = 'bash' | 'gitbash' | 'powershell' | 'sh'

export interface ResolvedShell {
  type: ShellType
  path: string
  args: string[]
}

// Windows 上 Git Bash 的常见安装位置
const GIT_BASH_PATHS = [
  'C:\\Program Files\\Git\\bin\\bash.exe',
  'C:\\Program Files (x86)\\Git\\bin\\bash.exe',
  `${process.env['LOCALAPPDATA'] ?? ''}\\Programs\\Git\\bin\\bash.exe`,
]

function findGitBash(): string | null {
  for (const p of GIT_BASH_PATHS) {
    if (existsSync(p)) return p
  }
  // PATH 中查找
  const pathEnv = process.env['PATH'] ?? ''
  for (const dir of pathEnv.split(';')) {
    const candidate = `${dir}\\bash.exe`
    if (existsSync(candidate)) return candidate
  }
  return null
}

export function resolveShell(preferred?: ShellType): ResolvedShell {
  const { isWindows } = detectPlatform()

  if (isWindows) {
    if (preferred !== 'powershell') {
      const gitBash = findGitBash()
      if (gitBash) {
        return { type: 'gitbash', path: gitBash, args: ['-c'] }
      }
    }
    return {
      type: 'powershell',
      path: 'powershell.exe',
      args: ['-NonInteractive', '-Command'],
    }
  }

  // Linux / macOS
  const bash = existsSync('/bin/bash') ? '/bin/bash' : '/bin/sh'
  return { type: bash.endsWith('bash') ? 'bash' : 'sh', path: bash, args: ['-c'] }
}
```

**Step 3: 写 detector 测试**

创建 `tests/unit/platform/detector.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { detectPlatform } from '@platform/detector.js'

describe('detectPlatform', () => {
  it('返回合法的 platform 信息', () => {
    const info = detectPlatform()
    expect(['win32', 'linux', 'darwin']).toContain(info.platform)
    expect(info.homeDir).toBeTruthy()
    expect(info.ccodeDir).toContain('.ccode')
  })

  it('isWindows / isLinux / isMac 三选一', () => {
    const { isWindows, isLinux, isMac } = detectPlatform()
    const trueCount = [isWindows, isLinux, isMac].filter(Boolean).length
    expect(trueCount).toBe(1)
  })
})
```

**Step 4: 写 shell-resolver 测试**

创建 `tests/unit/platform/shell-resolver.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { resolveShell } from '@platform/shell-resolver.js'

describe('resolveShell', () => {
  it('返回合法的 shell 信息', () => {
    const shell = resolveShell()
    expect(shell.path).toBeTruthy()
    expect(shell.args).toBeInstanceOf(Array)
    expect(shell.args.length).toBeGreaterThan(0)
  })

  it('shell type 是已知类型', () => {
    const shell = resolveShell()
    expect(['bash', 'gitbash', 'powershell', 'sh']).toContain(shell.type)
  })
})
```

**Step 5: 运行测试**

```bash
pnpm test
```

预期: `6 passed`

**Step 6: Commit**

```bash
git add src/platform/ tests/unit/platform/
git commit -m "feat: platform 检测与 Shell 自适应 (OS detector / shell resolver)"
```

---

## Task 7: CLI 入口 + 基础 Ink App

**Files:**
- Create: `bin/ccli.ts` — 完整入口
- Create: `src/ui/App.tsx` — 根组件
- Create: `src/ui/StatusBar.tsx` — 状态栏
- Create: `src/ui/ChatView.tsx` — 对话占位

**Step 1: 创建 src/ui/StatusBar.tsx**

```tsx
// src/ui/StatusBar.tsx
import React from 'react'
import { Box, Text } from 'ink'

interface StatusBarProps {
  model: string
  provider: string
  sessionId: string
}

export function StatusBar({ model, provider, sessionId }: StatusBarProps) {
  return (
    <Box borderStyle="single" paddingX={1}>
      <Text color="green">⚡ {model}</Text>
      <Text> · </Text>
      <Text color="cyan">{provider}</Text>
      <Text> · </Text>
      <Text dimColor>session: {sessionId.slice(0, 16)}</Text>
    </Box>
  )
}
```

**Step 2: 创建 src/ui/ChatView.tsx**

```tsx
// src/ui/ChatView.tsx
import React from 'react'
import { Box, Text } from 'ink'

export function ChatView() {
  return (
    <Box flexDirection="column" paddingX={1}>
      <Text dimColor>cCli 准备就绪，输入消息开始对话...</Text>
    </Box>
  )
}
```

**Step 3: 创建 src/ui/App.tsx**

```tsx
// src/ui/App.tsx
import React from 'react'
import { Box, Text, useApp } from 'ink'
import { StatusBar } from './StatusBar.js'
import { ChatView } from './ChatView.js'

interface AppProps {
  model?: string
  provider?: string
  sessionId?: string
}

export function App({
  model = 'claude-opus-4-6',
  provider = 'anthropic',
  sessionId = 'sess_00000000_000000_000',
}: AppProps) {
  const { exit } = useApp()

  return (
    <Box flexDirection="column" width="100%">
      <Box paddingX={1} marginBottom={1}>
        <Text bold color="blue">CCode</Text>
        <Text dimColor> — 多模型 AI 编程助手 v0.1.0</Text>
      </Box>

      <ChatView />

      <Box marginTop={1}>
        <StatusBar model={model} provider={provider} sessionId={sessionId} />
      </Box>
    </Box>
  )
}
```

**Step 4: 完善 bin/ccli.ts**

```typescript
// bin/ccli.ts
import React from 'react'
import { render } from 'ink'
import { App } from '../src/ui/App.js'

const { unmount } = render(React.createElement(App))

process.on('SIGINT', () => {
  unmount()
  process.exit(0)
})
```

**Step 5: 运行冒烟测试**

```bash
pnpm dev
```

预期: 终端显示 cCli 标题、ChatView 占位文字、底部 StatusBar，按 Ctrl+C 正常退出

**Step 6: Commit**

```bash
git add bin/ccli.ts src/ui/
git commit -m "feat: CLI 入口与基础 Ink UI (App / StatusBar / ChatView)"
```

---

## Task 8: 冒烟验证 + 收尾

**Step 1: 运行全量测试**

```bash
pnpm test
```

预期: 全部通过，无失败

**Step 2: TypeScript 类型检查**

```bash
pnpm typecheck
```

预期: 无报错

**Step 3: 构建验证**

```bash
pnpm build
```

预期: `dist/bin/ccli.js` 生成成功

**Step 4: 运行构建产物**

```bash
node dist/bin/ccli.js
```

预期: 与 `pnpm dev` 效果一致

**Step 5: 更新 .gitignore 补充构建产物**

确认 `dist/` 已在 .gitignore 中（之前已添加）

**Step 6: 最终 Commit**

```bash
git add -A
git commit -m "chore: 基础脚手架完成 — 项目可运行、测试通过、构建成功"
```

---

## 并行可能性分析

本阶段任务均有依赖链，需顺序执行：

```
Task 1 (配置)
  → Task 2 (目录)
    → Task 3 (安装依赖)
      → Task 4 (构建/测试配置)  ┐
      → Task 5 (类型定义)       ├── 可并行
      → Task 6 (Platform)       ┘
        → Task 7 (CLI 入口)
          → Task 8 (验证)
```

Task 4、5、6 完成后可并行推进，其余顺序依赖。
