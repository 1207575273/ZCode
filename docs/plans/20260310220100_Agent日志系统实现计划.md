# F9 Agent 日志系统实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不侵入业务代码的前提下，通过事件流 + 观察者模式记录 Agent 每轮执行的完整过程（LLM 调用、工具执行、MCP 连接、权限、异常、汇总），写入现有 session JSONL 文件。

**Architecture:** AgentLoop 扩展 AgentEvent 类型（新增 llm_start/llm_usage 等），Provider 改为返回 StreamChunk 中携带 usage 信息。独立的 SessionLogger 观察者订阅事件流并写入 JSONL。McpManager 通过 onConnect 回调通知日志。

**Tech Stack:** TypeScript, Vitest, 现有 persistence/session-store 基础设施

**设计文档:** `docs/plans/20260310220000_Agent日志系统设计.md`

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/persistence/session-types.ts` | 修改 | 扩展 SessionEventType + 新增字段 |
| `src/core/types.ts` | 修改 | StreamChunk 增加 usage 字段（已有，确认完整） |
| `src/core/agent-loop.ts` | 修改 | emit llm_start/llm_usage/llm_error/permission_grant/tool_fallback |
| `src/providers/anthropic.ts` | 修改 | 流结束后 yield usage chunk |
| `src/providers/openai-compat.ts` | 修改 | 流结束后 yield usage chunk |
| `src/mcp/mcp-manager.ts` | 修改 | 添加 onConnect 回调 |
| `src/observability/session-logger.ts` | **新建** | 事件流观察者，AgentEvent → SessionEvent → JSONL |
| `src/observability/index.ts` | **新建** | 导出 |
| `src/ui/useChat.ts` | 修改 | 集成 SessionLogger，迁移现有持久化逻辑 |
| `tests/unit/session-logger.test.ts` | **新建** | SessionLogger 测试 |
| `tests/unit/agent-loop.test.ts` | 修改 | 新增事件类型测试 |

---

## Task 1: 扩展 SessionEventType 和 SessionEvent 字段

**Files:**
- Modify: `zCli/src/persistence/session-types.ts`
- Test: `zCli/tests/unit/session-types.test.ts`

**说明:** 扩展事件类型枚举和 SessionEvent 接口字段，为后续 Task 打基础。

- [ ] **Step 1: 修改 SessionEventType 新增 10 种事件类型**

在 `session-types.ts` 的 `SessionEventType` 类型中新增：

```typescript
export type SessionEventType =
  // 已有
  | 'session_start' | 'session_resume' | 'user' | 'assistant' | 'system'
  | 'tool_call' | 'tool_result' | 'turn_duration'
  // F9 新增
  | 'llm_call_start' | 'llm_call_end'
  | 'tool_call_start' | 'tool_call_end'
  | 'mcp_connect_start' | 'mcp_connect_end'
  | 'tool_fallback'
  | 'permission_grant'
  | 'error'
  | 'session_end'
```

- [ ] **Step 2: 扩展 SessionEvent 接口新增可选字段**

在 `SessionEvent` 接口中追加：

```typescript
  // F9 新增字段
  /** LLM 调用相关 */
  inputTokens?: number
  outputTokens?: number
  stopReason?: string        // 'end_turn' | 'max_tokens' | 'abort' | 'error'
  messageCount?: number      // 发送给 LLM 的消息数

  /** 工具/MCP 相关 */
  success?: boolean
  resultSummary?: string     // 结果摘要（截断）
  serverName?: string
  transport?: string
  toolCount?: number         // MCP 发现的工具数

  /** 降级相关 */
  fromLevel?: string
  toLevel?: string
  reason?: string

  /** 权限相关 */
  always?: boolean

  /** 异常相关 */
  source?: string            // 'llm' | 'tool' | 'mcp' | 'system'
  stack?: string

  /** 会话汇总 (session_end) */
  totalInputTokens?: number
  totalOutputTokens?: number
  totalToolCalls?: number
  totalLlmCalls?: number
  totalErrors?: number
  totalDurationMs?: number
```

- [ ] **Step 3: 更新 session-types 测试**

在 `tests/unit/session-types.test.ts` 中新增测试，验证新事件类型可被正确赋值到 SessionEvent：

```typescript
describe('F9 SessionEventType extensions', () => {
  const F9_TYPES: SessionEventType[] = [
    'llm_call_start', 'llm_call_end',
    'tool_call_start', 'tool_call_end',
    'mcp_connect_start', 'mcp_connect_end',
    'tool_fallback', 'permission_grant', 'error', 'session_end',
  ]

  for (const eventType of F9_TYPES) {
    it(`should_accept_${eventType}_as_valid_SessionEventType`, () => {
      const event: SessionEvent = {
        sessionId: 'test', type: eventType, timestamp: new Date().toISOString(),
        uuid: 'u1', parentUuid: null, cwd: '/tmp',
      }
      expect(event.type).toBe(eventType)
    })
  }

  it('should_accept_new_optional_fields_on_SessionEvent', () => {
    const event: SessionEvent = {
      sessionId: 'test', type: 'llm_call_end', timestamp: new Date().toISOString(),
      uuid: 'u1', parentUuid: null, cwd: '/tmp',
      inputTokens: 100, outputTokens: 50, stopReason: 'end_turn',
      totalInputTokens: 500, totalOutputTokens: 200,
    }
    expect(event.inputTokens).toBe(100)
    expect(event.totalInputTokens).toBe(500)
  })
})
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/unit/session-types.test.ts`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add src/persistence/session-types.ts tests/unit/session-types.test.ts
git commit -m "feat(session): extend SessionEventType with F9 observability events"
```

---

## Task 2: 扩展 AgentEvent 类型

**Files:**
- Modify: `zCli/src/core/agent-loop.ts`（仅类型定义部分）
- Test: `zCli/tests/unit/agent-loop.test.ts`

**说明:** 在 AgentEvent 联合类型中新增 `llm_start`、`llm_usage`、`llm_error`、`tool_fallback`、`permission_grant` 五种事件。同时给现有 `tool_done` 增加 `resultSummary` 可选字段。

- [ ] **Step 1: 扩展 AgentEvent 类型定义**

在 `agent-loop.ts` 头部的 `AgentEvent` 类型中，追加新的联合分支：

```typescript
export type AgentEvent =
  // ── 已有 ──
  | { type: 'text';               text: string }
  | { type: 'tool_start';         toolName: string; toolCallId: string; args: Record<string, unknown> }
  | { type: 'tool_done';          toolName: string; toolCallId: string; durationMs: number; success: boolean; resultSummary?: string }
  | { type: 'permission_request'; toolName: string; args: Record<string, unknown>; resolve: (allow: boolean) => void }
  | { type: 'error';              error: string }
  | { type: 'done' }
  // ── F9 新增 ──
  | { type: 'llm_start';          provider: string; model: string; messageCount: number }
  | { type: 'llm_usage';          inputTokens: number; outputTokens: number; stopReason: string }
  | { type: 'llm_error';          error: string; partialOutputTokens?: number }
  | { type: 'tool_fallback';      toolName: string; fromLevel: string; toLevel: string; reason: string }
  | { type: 'permission_grant';   toolName: string; always: boolean }
```

- [ ] **Step 2: 写测试验证新事件类型可 yield**

在 `tests/unit/agent-loop.test.ts` 中新增：

```typescript
describe('AgentEvent F9 types', () => {
  it('should_type_check_llm_start_event', () => {
    const event: AgentEvent = { type: 'llm_start', provider: 'anthropic', model: 'claude', messageCount: 5 }
    expect(event.type).toBe('llm_start')
  })

  it('should_type_check_llm_usage_event', () => {
    const event: AgentEvent = { type: 'llm_usage', inputTokens: 100, outputTokens: 50, stopReason: 'end_turn' }
    expect(event.type).toBe('llm_usage')
  })

  it('should_type_check_llm_error_event', () => {
    const event: AgentEvent = { type: 'llm_error', error: 'timeout', partialOutputTokens: 20 }
    expect(event.type).toBe('llm_error')
  })

  it('should_type_check_tool_fallback_event', () => {
    const event: AgentEvent = { type: 'tool_fallback', toolName: 'bash', fromLevel: 'gitbash', toLevel: 'powershell', reason: 'not found' }
    expect(event.type).toBe('tool_fallback')
  })

  it('should_type_check_permission_grant_event', () => {
    const event: AgentEvent = { type: 'permission_grant', toolName: 'bash', always: true }
    expect(event.type).toBe('permission_grant')
  })

  it('should_accept_resultSummary_on_tool_done', () => {
    const event: AgentEvent = { type: 'tool_done', toolName: 'bash', toolCallId: 'tc1', durationMs: 100, success: true, resultSummary: 'file1.txt' }
    expect(event.type).toBe('tool_done')
  })
})
```

- [ ] **Step 3: 运行测试**

Run: `npx vitest run tests/unit/agent-loop.test.ts`
Expected: ALL PASS

- [ ] **Step 4: 提交**

```bash
git add src/core/agent-loop.ts tests/unit/agent-loop.test.ts
git commit -m "feat(core): extend AgentEvent with F9 observability types"
```

---

## Task 3: Provider 改造 — yield usage chunk

**Files:**
- Modify: `zCli/src/providers/anthropic.ts`
- Modify: `zCli/src/providers/openai-compat.ts`
- Test: `zCli/tests/unit/anthropic-provider.test.ts`
- Test: `zCli/tests/unit/openai-compat-provider.test.ts`

**说明:** 两个 Provider 在流结束前 yield 一个 `{ type: 'usage', usage: { ... } }` chunk。LangChain 的聚合 chunk（`final`）包含 `response_metadata.usage` 或 `usage_metadata`。同时在 catch 中 yield `error` chunk 时携带 partial token 信息。

- [ ] **Step 1: Anthropic Provider 提取 usage**

在 `anthropic.ts` 的 `chat` 方法中，流结束后、yield `done` 之前，提取 usage 并 yield：

```typescript
// 在 yield { type: 'done' } 之前插入：
if (allChunks.length > 0) {
  const final = allChunks.reduce((a: any, b: any) => a.concat(b))
  // 提取 usage（LangChain Anthropic 在 response_metadata 或 usage_metadata 中）
  const usageMeta = final.usage_metadata ?? final.response_metadata?.usage ?? null
  if (usageMeta) {
    yield {
      type: 'usage',
      usage: {
        inputTokens: usageMeta.input_tokens ?? usageMeta.prompt_tokens ?? 0,
        outputTokens: usageMeta.output_tokens ?? usageMeta.completion_tokens ?? 0,
        cacheReadTokens: usageMeta.cache_read_input_tokens ?? 0,
        cacheWriteTokens: usageMeta.cache_creation_input_tokens ?? 0,
      },
    }
  }
  // tool_calls 提取 (已有代码) ...
}
```

- [ ] **Step 2: OpenAI Compat Provider 提取 usage**

同样的模式添加到 `openai-compat.ts`：

```typescript
// 在 yield { type: 'done' } 之前插入：
if (allChunks.length > 0) {
  const final = allChunks.reduce((a: any, b: any) => a.concat(b))
  const usageMeta = final.usage_metadata ?? final.response_metadata?.usage ?? null
  if (usageMeta) {
    yield {
      type: 'usage',
      usage: {
        inputTokens: usageMeta.input_tokens ?? usageMeta.prompt_tokens ?? 0,
        outputTokens: usageMeta.output_tokens ?? usageMeta.completion_tokens ?? 0,
        cacheReadTokens: usageMeta.cache_read_input_tokens ?? 0,
        cacheWriteTokens: usageMeta.cache_creation_input_tokens ?? 0,
      },
    }
  }
  // tool_calls 提取 (已有代码) ...
}
```

- [ ] **Step 3: 更新 Provider 测试验证 usage chunk**

在 `tests/unit/anthropic-provider.test.ts` 和 `openai-compat-provider.test.ts` 中新增验证 usage chunk 的测试（使用现有的 mock stream 机制）。

- [ ] **Step 4: 运行测试**

Run: `npx vitest run tests/unit/anthropic-provider.test.ts tests/unit/openai-compat-provider.test.ts`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add src/providers/anthropic.ts src/providers/openai-compat.ts tests/unit/anthropic-provider.test.ts tests/unit/openai-compat-provider.test.ts
git commit -m "feat(providers): yield usage chunk with token statistics"
```

---

## Task 4: AgentLoop emit llm_start / llm_usage / llm_error

**Files:**
- Modify: `zCli/src/core/agent-loop.ts`
- Test: `zCli/tests/unit/agent-loop.test.ts`

**说明:** 在 AgentLoop 的 `run()` 方法中，LLM 调用前后 yield 观测事件。需要将 provider name 传入 AgentLoop（通过 AgentConfig 扩展）。

- [ ] **Step 1: AgentConfig 新增 provider 字段**

```typescript
interface AgentConfig {
  model: string
  provider: string  // 新增：provider 名称，用于 llm_start 事件
  signal?: AbortSignal
}
```

- [ ] **Step 2: 在 LLM 调用前后 yield 观测事件**

在 `run()` 方法中改造 LLM 调用循环：

```typescript
// LLM 调用前
yield { type: 'llm_start', provider: this.#config.provider, model: this.#config.model, messageCount: history.length }
const llmStartTime = Date.now()
let llmInputTokens = 0
let llmOutputTokens = 0
let llmStopReason = 'end_turn'
let llmError: string | undefined

try {
  for await (const chunk of this.#provider.chat(chatRequest)) {
    if (chunk.type === 'text' && chunk.text) {
      yield { type: 'text', text: chunk.text }
    } else if (chunk.type === 'tool_call' && chunk.toolCall) {
      pendingToolCalls.push(chunk.toolCall)
    } else if (chunk.type === 'usage' && chunk.usage) {
      llmInputTokens = chunk.usage.inputTokens
      llmOutputTokens = chunk.usage.outputTokens
    } else if (chunk.type === 'error') {
      llmError = chunk.error ?? 'unknown error'
      llmStopReason = 'error'
      yield { type: 'llm_error', error: llmError, partialOutputTokens: llmOutputTokens || undefined }
      yield { type: 'error', error: llmError }
      return
    }
  }

  // LLM 调用正常结束
  yield { type: 'llm_usage', inputTokens: llmInputTokens, outputTokens: llmOutputTokens, stopReason: llmStopReason }
} catch (err) {
  const msg = err instanceof Error ? err.message : String(err)
  if ((err as Error).name === 'AbortError') {
    yield { type: 'llm_usage', inputTokens: llmInputTokens, outputTokens: llmOutputTokens, stopReason: 'abort' }
  } else {
    yield { type: 'llm_error', error: msg, partialOutputTokens: llmOutputTokens || undefined }
  }
  throw err
}
```

- [ ] **Step 3: 在 permission 处理中 yield permission_grant**

在权限确认逻辑中，当用户允许时 yield：

```typescript
if (!allowed) {
  // ... 已有拒绝逻辑 ...
} else {
  // 记录权限授予
  const isAlways = false // useChat 层面处理 always，这里只记录单次授予
  yield { type: 'permission_grant', toolName: tc.toolName, always: isAlways }
}
```

- [ ] **Step 4: 在 tool_done 中添加 resultSummary**

```typescript
const resultSummary = result.success
  ? (result.output.length > 200 ? result.output.slice(0, 200) + '...' : result.output)
  : (result.error ?? 'error')

yield { type: 'tool_done', toolName: tc.toolName, toolCallId: tc.toolCallId, durationMs, success: result.success, resultSummary }
```

- [ ] **Step 5: 更新 useChat.ts 中 AgentLoop 构造 — 传入 provider**

在 `useChat.ts` 中：

```typescript
const loop = new AgentLoop(provider, registry, {
  model: currentModel,
  provider: currentProvider,  // 新增
  signal: controller.signal,
})
```

- [ ] **Step 6: 更新测试**

在 `tests/unit/agent-loop.test.ts` 中更新 AgentLoop 构造参数，确认 mock provider 场景下能收到 `llm_start` 和 `llm_usage` 事件。

- [ ] **Step 7: 运行测试**

Run: `npx vitest run tests/unit/agent-loop.test.ts`
Expected: ALL PASS

- [ ] **Step 8: 提交**

```bash
git add src/core/agent-loop.ts src/ui/useChat.ts tests/unit/agent-loop.test.ts
git commit -m "feat(core): AgentLoop emits llm_start/llm_usage/llm_error observability events"
```

---

## Task 5: McpManager onConnect 回调

**Files:**
- Modify: `zCli/src/mcp/mcp-manager.ts`
- Test: `zCli/tests/unit/mcp-manager.test.ts`（如存在则修改，否则新建）

**说明:** McpManager 在连接每个 server 前后触发 `onConnect` 回调，通知外部（SessionLogger）记录 `mcp_connect_start` / `mcp_connect_end` 事件。

- [ ] **Step 1: 定义 McpConnectEvent 类型并添加 onConnect 回调**

在 `mcp-manager.ts` 中：

```typescript
export interface McpConnectEvent {
  phase: 'start' | 'end'
  serverName: string
  transport: string    // 'stdio' | 'sse' | 'streamable-http'
  success?: boolean
  toolCount?: number
  durationMs?: number
  error?: string
}

export class McpManager {
  // 新增：连接事件回调
  onConnect?: (event: McpConnectEvent) => void
  // ... 已有字段 ...
```

- [ ] **Step 2: 在 connectAll 中触发回调**

在 `connectAll()` 的每个 server 连接循环中：

```typescript
const results = await Promise.allSettled(
  entries.map(async ([serverName, serverConfig]) => {
    const source = this.#serverSources[serverName] ?? 'unknown'
    const transport = this.#detectTransportType(serverConfig)
    const startTime = Date.now()

    // 连接前通知
    this.onConnect?.({ phase: 'start', serverName, transport })

    try {
      // ... 已有连接逻辑 ...
      // 连接后通知（成功）
      this.onConnect?.({ phase: 'end', serverName, transport, success: true, toolCount: tools.length, durationMs: Date.now() - startTime })
      return { server, info }
    } catch (err: unknown) {
      const message = err instanceof Error ? err.message : String(err)
      // 连接后通知（失败）
      this.onConnect?.({ phase: 'end', serverName, transport, success: false, error: message, durationMs: Date.now() - startTime })
      return { server: null, info }
    }
  }),
)
```

- [ ] **Step 3: 添加 #detectTransportType 辅助方法**

```typescript
#detectTransportType(config: McpServerConfig): string {
  if (config.command) return 'stdio'
  if (config.type === 'sse') return 'sse'
  return 'streamable-http'
}
```

- [ ] **Step 4: 运行类型检查和已有测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add src/mcp/mcp-manager.ts
git commit -m "feat(mcp): add onConnect callback for connection observability"
```

---

## Task 6: SessionLogger 核心实现

**Files:**
- Create: `zCli/src/observability/session-logger.ts`
- Create: `zCli/src/observability/index.ts`
- Test: `zCli/tests/unit/session-logger.test.ts`

**说明:** 这是 F9 的核心模块。SessionLogger 订阅 AgentEvent 流，将每个事件转换为 SessionEvent 并通过 sessionStore.append() 写入 JSONL。同时累计 TurnStats 用于 session_end 汇总。

- [ ] **Step 1: 定义 TurnStats 类型和 SessionLogger 类**

```typescript
// src/observability/session-logger.ts

import { sessionStore, generateEventId } from '@persistence/index.js'
import type { SessionEvent, SessionEventType } from '@persistence/session-types.js'
import type { AgentEvent } from '@core/agent-loop.js'
import type { McpConnectEvent } from '@mcp/mcp-manager.js'

interface TurnStats {
  totalInputTokens: number
  totalOutputTokens: number
  totalToolCalls: number
  totalLlmCalls: number
  totalErrors: number
  startTime: number
}

export class SessionLogger {
  #sessionId: string | null = null
  #lastEventUuid: string | null = null
  #turnStats: TurnStats = this.#emptyStats()
  #cwd: string = process.cwd()

  /** 绑定到一个会话 */
  bind(sessionId: string, lastEventUuid?: string | null): void {
    this.#sessionId = sessionId
    this.#lastEventUuid = lastEventUuid ?? null
  }

  /** 获取当前 sessionId */
  get sessionId(): string | null {
    return this.#sessionId
  }

  /** 获取最新事件 UUID（供 parentUuid 链接） */
  get lastEventUuid(): string | null {
    return this.#lastEventUuid
  }

  /** 消费一个 AgentEvent */
  consume(event: AgentEvent): void {
    if (!this.#sessionId) return

    switch (event.type) {
      case 'llm_start':
        this.#turnStats.totalLlmCalls++
        this.#appendEvent('llm_call_start', {
          provider: event.provider,
          model: event.model,
          messageCount: event.messageCount,
        })
        break

      case 'llm_usage':
        this.#turnStats.totalInputTokens += event.inputTokens
        this.#turnStats.totalOutputTokens += event.outputTokens
        this.#appendEvent('llm_call_end', {
          inputTokens: event.inputTokens,
          outputTokens: event.outputTokens,
          stopReason: event.stopReason,
        })
        break

      case 'llm_error':
        this.#turnStats.totalErrors++
        this.#appendEvent('llm_call_end', {
          outputTokens: event.partialOutputTokens ?? 0,
          stopReason: 'error',
          error: event.error,
        })
        break

      case 'tool_start':
        this.#turnStats.totalToolCalls++
        this.#appendEvent('tool_call_start', {
          toolName: event.toolName,
          toolCallId: event.toolCallId,
          args: event.args,
        })
        break

      case 'tool_done':
        this.#appendEvent('tool_call_end', {
          toolName: event.toolName,
          toolCallId: event.toolCallId,
          durationMs: event.durationMs,
          success: event.success,
          resultSummary: event.resultSummary,
        })
        break

      case 'tool_fallback':
        this.#appendEvent('tool_fallback', {
          toolName: event.toolName,
          fromLevel: event.fromLevel,
          toLevel: event.toLevel,
          reason: event.reason,
        })
        break

      case 'permission_grant':
        this.#appendEvent('permission_grant', {
          toolName: event.toolName,
          always: event.always,
        })
        break

      case 'error':
        this.#turnStats.totalErrors++
        this.#appendEvent('error', {
          error: event.error,
          source: 'agent',
        })
        break

      // text, done, permission_request 不写日志
      default:
        break
    }
  }

  /** 记录用户消息 */
  logUserMessage(content: string): void {
    this.#appendEvent('user', {
      message: { role: 'user', content },
    })
  }

  /** 记录助手回复 */
  logAssistantMessage(content: string, model?: string): void {
    this.#appendEvent('assistant', {
      message: { role: 'assistant', content, model },
    })
  }

  /** 记录 MCP 连接事件 */
  logMcpConnect(event: McpConnectEvent): void {
    if (!this.#sessionId) return
    const type: SessionEventType = event.phase === 'start' ? 'mcp_connect_start' : 'mcp_connect_end'
    this.#appendEvent(type, {
      serverName: event.serverName,
      transport: event.transport,
      success: event.success,
      toolCount: event.toolCount,
      durationMs: event.durationMs,
      error: event.error,
    })
  }

  /** 创建新会话（惰性，首次 submit 时调用） */
  ensureSession(provider: string, model: string): string {
    if (this.#sessionId) return this.#sessionId
    try {
      const id = sessionStore.create(this.#cwd, provider, model)
      this.#sessionId = id
      this.#lastEventUuid = null
      this.#turnStats = this.#emptyStats()
      return id
    } catch {
      return ''
    }
  }

  /** 写入 session_end 汇总事件 */
  finalize(): void {
    if (!this.#sessionId) return
    const durationMs = Date.now() - this.#turnStats.startTime
    this.#appendEvent('session_end', {
      totalInputTokens: this.#turnStats.totalInputTokens,
      totalOutputTokens: this.#turnStats.totalOutputTokens,
      totalToolCalls: this.#turnStats.totalToolCalls,
      totalLlmCalls: this.#turnStats.totalLlmCalls,
      totalErrors: this.#turnStats.totalErrors,
      totalDurationMs: durationMs,
    })
  }

  /** 追加事件到 JSONL，静默处理错误 */
  #appendEvent(type: SessionEventType, fields: Partial<SessionEvent> = {}): void {
    if (!this.#sessionId) return
    try {
      const eventId = generateEventId()
      const event: SessionEvent = {
        sessionId: this.#sessionId,
        type,
        timestamp: new Date().toISOString(),
        uuid: eventId,
        parentUuid: this.#lastEventUuid,
        cwd: this.#cwd,
        ...fields,
      }
      sessionStore.append(this.#sessionId, event)
      this.#lastEventUuid = eventId
    } catch {
      // 持久化失败不阻断主流程
    }
  }

  #emptyStats(): TurnStats {
    return {
      totalInputTokens: 0,
      totalOutputTokens: 0,
      totalToolCalls: 0,
      totalLlmCalls: 0,
      totalErrors: 0,
      startTime: Date.now(),
    }
  }
}
```

- [ ] **Step 2: 创建 index.ts 导出**

```typescript
// src/observability/index.ts
export { SessionLogger } from './session-logger.js'
```

- [ ] **Step 3: 写 SessionLogger 单元测试**

创建 `tests/unit/session-logger.test.ts`：

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { mkdtempSync, rmSync, readFileSync } from 'node:fs'
import { join } from 'node:path'
import { tmpdir } from 'node:os'
import { SessionLogger } from '@observability/session-logger'
import { SessionStore } from '@persistence/session-store'
import type { SessionEvent } from '@persistence/session-types'
import type { AgentEvent } from '@core/agent-loop'

// 用临时目录创建独立的 SessionStore + SessionLogger
let tempDir: string
let store: SessionStore
let logger: SessionLogger

beforeEach(() => {
  tempDir = mkdtempSync(join(tmpdir(), 'session-logger-test-'))
  // 需要在测试中用独立 store（不用全局单例）
  // SessionLogger 构造函数需要支持注入 store
})

afterEach(() => {
  rmSync(tempDir, { recursive: true, force: true })
})

// 辅助：读取 JSONL 文件中的所有事件
function readEvents(filePath: string): SessionEvent[] {
  const content = readFileSync(filePath, 'utf-8')
  return content.trim().split('\n').filter(Boolean).map(l => JSON.parse(l))
}

describe('SessionLogger.consume', () => {
  it('should_write_llm_call_start_event', () => { /* ... */ })
  it('should_write_llm_call_end_event_with_tokens', () => { /* ... */ })
  it('should_write_llm_call_end_with_error_on_llm_error', () => { /* ... */ })
  it('should_write_tool_call_start_and_end_events', () => { /* ... */ })
  it('should_write_tool_fallback_event', () => { /* ... */ })
  it('should_write_permission_grant_event', () => { /* ... */ })
  it('should_write_error_event', () => { /* ... */ })
  it('should_accumulate_turn_stats', () => { /* ... */ })
  it('should_chain_parentUuid_correctly', () => { /* ... */ })
  it('should_silently_ignore_events_when_no_session_bound', () => { /* ... */ })
})

describe('SessionLogger.logMcpConnect', () => {
  it('should_write_mcp_connect_start_event', () => { /* ... */ })
  it('should_write_mcp_connect_end_success_event', () => { /* ... */ })
  it('should_write_mcp_connect_end_error_event', () => { /* ... */ })
})

describe('SessionLogger.finalize', () => {
  it('should_write_session_end_with_accumulated_stats', () => { /* ... */ })
})
```

**注意：** SessionLogger 当前直接 import 全局 `sessionStore`。为了测试，需要改为构造函数注入 `SessionStore` 实例。在 SessionLogger 构造函数中添加可选参数：

```typescript
constructor(store?: SessionStore) {
  this.#store = store ?? sessionStore  // 默认用全局单例，测试时注入
}
```

- [ ] **Step 4: 运行测试**

Run: `npx vitest run tests/unit/session-logger.test.ts`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add src/observability/session-logger.ts src/observability/index.ts tests/unit/session-logger.test.ts
git commit -m "feat(observability): implement SessionLogger — event stream observer for JSONL"
```

---

## Task 7: useChat 集成 SessionLogger

**Files:**
- Modify: `zCli/src/ui/useChat.ts`

**说明:** 将 useChat.ts 中散落的持久化逻辑（`appendSessionEvent`、`ensureSession`、`sessionIdRef`、`lastEventUuidRef`）迁移到 SessionLogger，useChat 只保留 `sessionLogger.consume(event)` 一行。模块级 `currentSessionId` 改为从 SessionLogger 读取。

- [ ] **Step 1: 创建模块级 SessionLogger 实例**

替换 useChat.ts 中的：
- `sessionIdRef` → `sessionLogger.sessionId`
- `lastEventUuidRef` → `sessionLogger.lastEventUuid`
- `ensureSession()` → `sessionLogger.ensureSession()`
- `appendSessionEvent()` → 删除，由 `sessionLogger.consume()` 接管
- `currentSessionId` 模块级变量 → `sessionLogger.sessionId`

```typescript
import { SessionLogger } from '@observability/index.js'

// 模块级 SessionLogger 实例
const sessionLogger = new SessionLogger()

export function getCurrentSessionId(): string | null {
  return sessionLogger.sessionId
}
```

- [ ] **Step 2: 在 AgentEvent 消费循环中加入 sessionLogger.consume**

```typescript
for await (const event of loop.run(history)) {
  // 日志记录（新增一行）
  sessionLogger.consume(event)

  // UI 更新（已有）
  if (event.type === 'text') { ... }
  // ...
}
```

- [ ] **Step 3: 迁移 user/assistant 消息的持久化**

替换 submit 函数中的手动 `appendSessionEvent` 为：

```typescript
// 提交前记录用户消息
sessionLogger.ensureSession(currentProvider, currentModel)
sessionLogger.logUserMessage(text)

// 流结束后记录助手回复
if (accumulated) {
  sessionLogger.logAssistantMessage(accumulated, currentModel)
}
```

- [ ] **Step 4: 迁移 loadSession 和 forkFromEvent**

`loadSession` 中：
```typescript
sessionLogger.bind(sessionId, snapshot.leafEventUuid)
```

`forkFromEvent` 中：
```typescript
sessionLogger.bind(sid, messageId)
```

- [ ] **Step 5: 挂载 MCP onConnect 回调**

在 `ensureMcpInitialized` 中：

```typescript
mcpManager.onConnect = (event) => sessionLogger.logMcpConnect(event)
```

- [ ] **Step 6: 在退出时 finalize**

在 `zcli.ts` 的 `exitGracefully` 或 `process.on('exit')` 中调用：

```typescript
import { sessionLogger } from '@observability/index.js'
// 退出前写入 session_end
sessionLogger.finalize()
```

这需要在 `observability/index.ts` 中导出模块级实例。

- [ ] **Step 7: 运行全部测试**

Run: `npx vitest run`
Expected: ALL PASS（现有 154 个测试 + 新增测试）

- [ ] **Step 8: 提交**

```bash
git add src/ui/useChat.ts src/observability/index.ts bin/zcli.ts
git commit -m "feat(chat): integrate SessionLogger — replace scattered persistence with event observer"
```

---

## Task 8: 端到端验证和清理

**Files:**
- All modified files
- Modify: `zCli/tests/unit/session-store.test.ts`（验证新事件类型的 round-trip）

**说明:** 全量测试、类型检查、确认无回归。

- [ ] **Step 1: 运行 TypeScript 类型检查**

Run: `npx tsc --noEmit`
Expected: 零错误

- [ ] **Step 2: 运行全量测试**

Run: `npx vitest run`
Expected: ALL PASS

- [ ] **Step 3: 手动 smoke test**

启动 `pnpm dev`，发送一条消息，Ctrl+C 退出，检查 JSONL 文件中是否包含 `llm_call_start`、`llm_call_end`、`session_end` 等新事件。

- [ ] **Step 4: 清理 debug.ts 中的调试日志（如有残留）**

确认 Provider 中的 `dbg()` 调用不影响新逻辑。

- [ ] **Step 5: 提交清理**

```bash
git commit -m "chore: F9 端到端验证通过，清理残留"
```

---

## 依赖关系

```
Task 1 (SessionEventType 扩展)
  ↓
Task 2 (AgentEvent 扩展)      Task 5 (McpManager onConnect)
  ↓                               ↓
Task 3 (Provider usage)           │
  ↓                               │
Task 4 (AgentLoop emit)           │
  ↓                               ↓
Task 6 (SessionLogger 核心) ←────────
  ↓
Task 7 (useChat 集成)
  ↓
Task 8 (端到端验证)
```

**可并行执行：** Task 2 + Task 5（无依赖关系），Task 3 依赖 Task 2 完成后可与 Task 5 并行。
