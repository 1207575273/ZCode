# ZCli 欢迎界面 UI 重构实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 CLI 启动界面重构为 Claude Code 风格的欢迎屏，首次输入后切换为聊天视图。

**Architecture:** App 持有 `started` + `messages` 状态；started=false 时渲染 WelcomeScreen，started=true 后渲染 ChatView；InputBar 提取为独立组件始终显示在底部。StatusBar 删除。

**Tech Stack:** Ink 5, React 18, ink-text-input, TypeScript strict

---

## Task 1: 提取 InputBar 组件

**Files:**
- Create: `src/ui/InputBar.tsx`
- Modify: `src/ui/ChatView.tsx`

**Step 1: 创建 `src/ui/InputBar.tsx`**

```tsx
// src/ui/InputBar.tsx
import React, { useState } from 'react'
import { Box, Text } from 'ink'
import TextInput from 'ink-text-input'

interface InputBarProps {
  onSubmit: (value: string) => void
  placeholder?: string
}

export function InputBar({ onSubmit, placeholder = 'Try "how does <filepath> work?"' }: InputBarProps) {
  const [value, setValue] = useState('')

  function handleSubmit(text: string) {
    const trimmed = text.trim()
    if (!trimmed) return
    onSubmit(trimmed)
    setValue('')
  }

  return (
    <Box flexDirection="column">
      <Box>
        <Text dimColor>{'─'.repeat(60)}</Text>
      </Box>
      <Box paddingLeft={1}>
        <Text color="green">❯ </Text>
        <TextInput
          value={value}
          onChange={setValue}
          onSubmit={handleSubmit}
          placeholder={placeholder}
        />
      </Box>
    </Box>
  )
}
```

**Step 2: 精简 ChatView（移除输入逻辑，只保留消息列表）**

```tsx
// src/ui/ChatView.tsx
import React from 'react'
import { Box, Text } from 'ink'

interface ChatMessage {
  role: 'user' | 'assistant'
  content: string
}

interface ChatViewProps {
  messages: ChatMessage[]
}

export function ChatView({ messages }: ChatViewProps) {
  return (
    <Box flexDirection="column" paddingX={1} flexGrow={1}>
      {messages.map((msg, i) => (
        <Box key={i} marginBottom={1} flexDirection="column">
          <Text color={msg.role === 'user' ? 'green' : 'cyan'} bold>
            {msg.role === 'user' ? '> 你' : '◆ ZCli'}
          </Text>
          <Text>{msg.content}</Text>
        </Box>
      ))}
    </Box>
  )
}
```

**Step 3: 运行类型检查**

```bash
cd D:/a_dev_work/claude_cli_z01/zCli
pnpm typecheck
```

预期：无报错（此时 App.tsx 还未更新，会有 import 错误，忽略 StatusBar 相关错误，Task 3 修复）

**Step 4: Commit**

```bash
cd D:/a_dev_work/claude_cli_z01
git add zCli/src/ui/InputBar.tsx zCli/src/ui/ChatView.tsx
git commit -m "refactor: 提取 InputBar 组件，ChatView 只负责消息列表"
```

---

## Task 2: 创建 WelcomeScreen 组件

**Files:**
- Create: `src/ui/WelcomeScreen.tsx`

**Step 1: 创建 `src/ui/WelcomeScreen.tsx`**

```tsx
// src/ui/WelcomeScreen.tsx
import React from 'react'
import { Box, Text } from 'ink'

// ASCII 机器人艺术字（模仿 Claude Code 像素风格）
const ROBOT_ART = [
  '  ▄██████▄  ',
  '  █ ■  ■ █  ',
  '  █  ▄▄  █  ',
  '  ▀██████▀  ',
  '   ██  ██   ',
]

interface WelcomeScreenProps {
  model: string
  provider: string
  cwd: string
}

export function WelcomeScreen({ model, provider, cwd }: WelcomeScreenProps) {
  return (
    <Box
      borderStyle="round"
      borderColor="red"
      flexDirection="column"
      marginX={1}
    >
      {/* 标题行 */}
      <Box paddingX={1} marginBottom={1}>
        <Text color="red" bold>── ZCli v0.1.0 ──</Text>
      </Box>

      {/* 双栏主体 */}
      <Box flexDirection="row" flexGrow={1}>
        {/* 左栏：用户信息 + ASCII 机器人 */}
        <Box
          flexDirection="column"
          width={28}
          paddingLeft={2}
          paddingRight={1}
          borderRight
        >
          <Text bold color="white">Welcome back!</Text>
          <Box flexDirection="column" marginY={1}>
            {ROBOT_ART.map((line, i) => (
              <Text key={i} color="red">{line}</Text>
            ))}
          </Box>
          <Text color="white">{model}</Text>
          <Text dimColor>{provider}</Text>
          <Text dimColor>{cwd}</Text>
        </Box>

        {/* 右栏：提示 + 最近记录 */}
        <Box flexDirection="column" flexGrow={1} paddingLeft={2}>
          <Text color="yellow" bold>Tips for getting started</Text>
          <Text>输入 <Text color="cyan">/help</Text> 查看可用命令</Text>
          <Text>输入 <Text color="cyan">/model</Text> 切换模型</Text>
          <Box marginTop={1} flexDirection="column">
            <Text color="yellow" bold>Recent sessions</Text>
            <Text dimColor>No recent activity</Text>
          </Box>
        </Box>
      </Box>
    </Box>
  )
}
```

**Step 2: 运行类型检查**

```bash
cd D:/a_dev_work/claude_cli_z01/zCli
pnpm typecheck 2>&1 | head -30
```

预期：WelcomeScreen 本身无类型错误（App.tsx 的错误此步可忽略）

**Step 3: Commit**

```bash
cd D:/a_dev_work/claude_cli_z01
git add zCli/src/ui/WelcomeScreen.tsx
git commit -m "feat: 新增 WelcomeScreen 双栏欢迎界面"
```

---

## Task 3: 重写 App.tsx，连接所有组件

**Files:**
- Modify: `src/ui/App.tsx`
- Delete: `src/ui/StatusBar.tsx`

**Step 1: 重写 `src/ui/App.tsx`**

```tsx
// src/ui/App.tsx
import React, { useState } from 'react'
import { Box, useApp } from 'ink'
import { WelcomeScreen } from './WelcomeScreen.js'
import { ChatView } from './ChatView.js'
import { InputBar } from './InputBar.js'

interface ChatMessage {
  role: 'user' | 'assistant'
  content: string
}

interface AppProps {
  model?: string
  provider?: string
  cwd?: string
}

export function App({
  model = 'claude-opus-4-6',
  provider = 'anthropic',
  cwd = process.cwd(),
}: AppProps) {
  const { exit } = useApp()
  const [started, setStarted] = useState(false)
  const [messages, setMessages] = useState<ChatMessage[]>([])

  function handleSubmit(text: string) {
    if (text === '/exit' || text === '/quit') {
      exit()
      return
    }
    setMessages(prev => [...prev, { role: 'user', content: text }])
    if (!started) setStarted(true)
  }

  return (
    <Box flexDirection="column" width="100%">
      {started ? (
        <ChatView messages={messages} />
      ) : (
        <WelcomeScreen model={model} provider={provider} cwd={cwd} />
      )}
      <InputBar onSubmit={handleSubmit} />
    </Box>
  )
}
```

**Step 2: 删除 StatusBar.tsx**

```bash
rm D:/a_dev_work/claude_cli_z01/zCli/src/ui/StatusBar.tsx
```

**Step 3: 运行类型检查（必须全部通过）**

```bash
cd D:/a_dev_work/claude_cli_z01/zCli
pnpm typecheck
```

预期：exit code 0，无报错

**Step 4: 运行测试（确保无回归）**

```bash
cd D:/a_dev_work/claude_cli_z01/zCli
pnpm test
```

预期：8 passed

**Step 5: 冒烟验证（启动看 UI）**

```bash
cd D:/a_dev_work/claude_cli_z01/zCli
node --import tsx/esm bin/zcli.ts
```

预期：显示欢迎屏（双栏布局 + ASCII 机器人 + 底部输入框）

**Step 6: Commit**

```bash
cd D:/a_dev_work/claude_cli_z01
git rm zCli/src/ui/StatusBar.tsx
git add zCli/src/ui/App.tsx
git commit -m "feat: 重构 App — 欢迎屏/聊天视图切换，移除 StatusBar"
```

---

## Task 4: 冒烟 + 收尾验证

**Step 1: 全量测试**

```bash
cd D:/a_dev_work/claude_cli_z01/zCli
pnpm test
```

预期：8 passed

**Step 2: 类型检查**

```bash
pnpm typecheck
```

预期：无报错

**Step 3: 构建**

```bash
pnpm build
```

预期：dist/bin/zcli.js 生成成功

**Step 4: 最终 Commit**

```bash
cd D:/a_dev_work/claude_cli_z01
git add -A
git status
# 若有未提交变更则提交
git commit -m "chore: 欢迎界面 UI 重构完成验证" 2>/dev/null || echo "nothing to commit"
```

---

## 任务依赖

```
Task 1 (InputBar + ChatView)
  → Task 2 (WelcomeScreen)
    → Task 3 (App 重写 + StatusBar 删除)
      → Task 4 (验证)
```
