# F10 Token 计量计费实现总结

## 概述

F10 为 ZCli 引入了 **Token 计量计费系统**，在每次 LLM 调用后自动记录四维 token 消耗（input/output/cacheRead/cacheWrite）到 SQLite，匹配计价规则计算费用，并在 StatusBar 和 `/usage` 指令中展示。

**核心设计**：AgentLoop 产生 `llm_usage` 事件 → TokenMeter 独立观察者消费 → 匹配 pricing_rules → 写入 usage_logs → 维护会话统计。

---

## 架构

```
AgentLoop                   SessionLogger (F9)          SessionStore → JSONL
  │                             │
  ├─ yield llm_usage ──────────► consume()
  │       │
  │       └────────────────────► TokenMeter (F10)        SQLite → usage_logs
  │                                 │
  │                                 ├─ #resolveRule()    → pricing_rules 匹配
  │                                 ├─ #calculateCost()  → 四维费用计算
  │                                 ├─ getSessionStats() → 内存累计（StatusBar）
  │                                 ├─ getTodayStats()   → SQL 聚合
  │                                 └─ getMonthStats()   → SQL 聚合
  │
  └─ yield text/done ─────────► UI 渲染
```

**关键特征**：
- **独立观察者**：TokenMeter 与 SessionLogger 平级，互不依赖，各自消费 AgentEvent
- **四维 token**：input / output / cacheRead / cacheWrite 全链路贯通
- **懒加载 SQLite**：首次 `getDb()` 时创建 `~/.zcli/data/zcli.db`
- **内聚计价**：pricing_rules 匹配和费用计算全部封装在 TokenMeter 内部
- **prepared statement 缓存**：构造时预编译 SQL，规则匹配按 provider+model 缓存
- **本地时区**：getTodayStats / getMonthStats 使用本地日期计算边界

---

## 数据库设计

### usage_logs 表

每次 LLM 调用产生一条记录，记录四维 token 消耗和费用。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| session_id | TEXT NOT NULL | 所属会话 ID |
| timestamp | TEXT NOT NULL | 记录时间 (ISO 8601) |
| provider | TEXT NOT NULL | LLM 供应商 |
| model | TEXT NOT NULL | 模型标识 |
| input_tokens | INTEGER NOT NULL | 输入 token 数 |
| output_tokens | INTEGER NOT NULL | 输出 token 数 |
| cache_read | INTEGER NOT NULL | 缓存读取 token 数 |
| cache_write | INTEGER NOT NULL | 缓存写入 token 数 |
| duration_ms | INTEGER | LLM 调用耗时（毫秒） |
| cost_amount | REAL | 费用（美元），无匹配规则时为 NULL |
| cost_currency | TEXT NOT NULL | 费用币种，默认 USD |
| pricing_rule_id | INTEGER | 匹配的计价规则 ID |

### pricing_rules 表

按 provider + model 通配符匹配，支持时间范围和优先级。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| provider | TEXT NOT NULL | LLM 供应商 |
| model_pattern | TEXT NOT NULL | 匹配模式（末尾 `*` 通配符） |
| input_price | REAL NOT NULL | 输入价格 ($/百万 token) |
| output_price | REAL NOT NULL | 输出价格 ($/百万 token) |
| cache_read_price | REAL NOT NULL | 缓存读取价格 ($/百万 token) |
| cache_write_price | REAL NOT NULL | 缓存写入价格 ($/百万 token) |
| currency | TEXT NOT NULL | 价格币种，默认 USD |
| effective_from | TEXT NOT NULL | 生效起始日期 |
| effective_to | TEXT | 截止日期（NULL 表示永久有效） |
| source | TEXT | 价格来源说明 |
| priority | INTEGER NOT NULL | 匹配优先级（越大越优先） |

### schema_comments 表

SQLite 不支持列级 COMMENT，用此表存储字段说明，可运行时查询。

| 字段 | 类型 | 说明 |
|------|------|------|
| table_name | TEXT NOT NULL | 所属表名 |
| column_name | TEXT NOT NULL | 字段名 |
| comment | TEXT NOT NULL | 字段说明 |

### 内置默认计价规则（11 条）

| 供应商 | 模式 | 输入 $/M | 输出 $/M | 缓存读 $/M | 缓存写 $/M |
|--------|------|---------|---------|-----------|-----------|
| anthropic | claude-opus-4-* | 15.0 | 75.0 | 1.50 | 18.75 |
| anthropic | claude-sonnet-4-* | 3.0 | 15.0 | 0.30 | 3.75 |
| anthropic | claude-haiku-* | 0.8 | 4.0 | 0.08 | 1.00 |
| openai | gpt-4o | 2.5 | 10.0 | 1.25 | 0.0 |
| openai | gpt-4o-mini | 0.15 | 0.6 | 0.075 | 0.0 |
| openai | o3-mini | 1.1 | 4.4 | 0.55 | 0.0 |
| google | gemini-2.0-flash* | 0.1 | 0.4 | 0.0 | 0.0 |
| google | gemini-2.5-pro* | 1.25 | 10.0 | 0.0 | 0.0 |
| deepseek | deepseek-r1* | 0.55 | 2.19 | 0.0 | 0.0 |
| deepseek | deepseek-v3* | 0.27 | 1.10 | 0.0 | 0.0 |
| ollama | * | 0.0 | 0.0 | 0.0 | 0.0 |

---

## 文件清单

### 新增文件 (5)

| 文件 | 行数 | 职责 |
|------|------|------|
| `src/persistence/db.ts` | ~170 | SQLite 懒加载单例 + 建表 + 默认计价 + schema_comments |
| `src/observability/token-meter.ts` | ~200 | 核心：consume → 计价匹配 → 写 usage_logs → 会话/日/月统计 |
| `src/commands/usage.ts` | ~18 | `/usage` 指令定义 |
| `tests/unit/db.test.ts` | ~60 | db 模块测试（5 个） |
| `tests/unit/token-meter.test.ts` | ~120 | TokenMeter 测试（8 个） |

### 修改文件 (11)

| 文件 | 改动摘要 |
|------|---------|
| `src/core/agent-loop.ts` | `llm_usage` 事件新增 `cacheReadTokens` / `cacheWriteTokens` |
| `src/persistence/session-types.ts` | SessionEvent 新增 4 个缓存 token 可选字段 |
| `src/observability/session-logger.ts` | TurnStats + consume + finalize 支持缓存 token 累计 |
| `src/observability/index.ts` | 导出 TokenMeter + SessionCostStats + AggregateStats |
| `src/persistence/index.ts` | 导出 getDb / createDb / closeDb |
| `src/ui/useChat.ts` | 集成 tokenMeter.consume() + bind() |
| `src/ui/App.tsx` | 注册 /usage、处理 show_usage、StatusBar token 展示 |
| `src/commands/types.ts` | CommandAction 新增 `show_usage` |
| `bin/zcli.ts` | 退出时 closeDb() |
| `tests/unit/session-logger.test.ts` | llm_usage 事件补充 cache 字段 |
| `tests/unit/session-logger-e2e.test.ts` | 同上 |

---

## 费用计算公式

```
cost = (inputTokens × input_price
      + outputTokens × output_price
      + cacheReadTokens × cache_read_price
      + cacheWriteTokens × cache_write_price) / 1,000,000
```

计价规则匹配逻辑：
1. 按 `provider` 精确匹配
2. 按 `effective_from <= now` 且 `effective_to IS NULL OR > now` 过滤时间范围
3. 按 `priority DESC, effective_from DESC` 排序
4. 逐条用 `model_pattern` 通配符匹配（仅支持末尾 `*`）
5. 首个匹配命中即返回，无匹配则 cost 为 NULL

---

## 测试覆盖

### db.test.ts（5 个）
- 建表验证：usage_logs / pricing_rules / schema_comments
- 默认计价规则 seed 验证
- schema_comments 字段注释验证
- 幂等性验证（二次调用不重复插入）

### token-meter.test.ts（8 个）
- 写入 usage_log 记录
- 基础计价（input + output）
- 缓存 token 计价（cacheRead + cacheWrite）
- 四维同时非零的完整计价
- 无匹配规则时 cost 为 NULL
- 忽略非 llm_usage 事件
- 会话累计统计
- 今日汇总 SQL 查询

---

## 提交历史

| Commit | 描述 |
|--------|------|
| `ca67870` | feat(core): extend llm_usage with cache token dimensions |
| `d15fa83` | feat(persistence): SQLite lazy singleton with default pricing rules |
| `558cb88` | feat(observability): TokenMeter — independent observer for usage billing |
| `0fd7769` | feat(chat): integrate TokenMeter into event consumption loop |
| `dd98f8c` | feat(ui): /usage command and StatusBar token display |
| `bf6ee1f` | feat(persistence): add schema_comments table for column-level metadata |
| `0cabf5c` | fix(observability): cache prepared statements, fix timezone, add cache token tests |

---

## 决策记录

1. **TokenMeter 独立于 SessionLogger**：两者平级消费 AgentEvent，各自负责 SQLite 和 JSONL。好处：完全解耦，可独立测试，关注点分离。

2. **内置默认计价规则**：hardcode 在代码中，首次建库时 seed 到 SQLite。好处：开箱即用，无需外部配置。用 `INSERT WHERE NOT EXISTS` 保证幂等。

3. **better-sqlite3 同步 API**：CLI 场景下写入频率低（每次 LLM 调用一次），微秒级同步写入不阻塞。相比 async 方案代码更简洁，无需处理 Promise。

4. **零新目录**：用户明确要求减少新建文件夹。计价逻辑内聚在 TokenMeter（私有方法），db.ts 放在已有的 persistence/ 下。

5. **prepared statement 缓存**：构造时预编译 4 条 SQL，pricing rule 按 provider+model 缓存。bind() 切换时自动失效。避免每次 consume 重复解析 SQL。

6. **本地时区**："今日"和"本月"使用本地日期计算，符合用户直觉（UTC+8 凌晨不会看到"昨天"的数据）。

7. **schema_comments 元数据表**：SQLite 不支持列级 COMMENT，单独建表存储字段说明，可运行时查询 `SELECT * FROM schema_comments WHERE table_name = 'usage_logs'`。
